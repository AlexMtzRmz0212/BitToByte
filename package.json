{
  "name": "bittobyte-monorepo",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.10.0",
  "description": "BitToByte web ecosystem — shared UI library, company landing page, and personal portfolio.",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "preview": "turbo run preview",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,css,md,json}\""
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.5.0"
  }
}
